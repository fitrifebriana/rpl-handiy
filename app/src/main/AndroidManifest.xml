<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.handiy.handiy">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".signin.SigninActivity" />
        <activity android:name=".creation.CreationActivity" />
        <activity android:name=".detail.DetailActivity" />
        <activity android:name=".furniture.FurnitureActivity" />
        <activity android:name=".decoration.DecorationActivity" />
        <activity android:name=".wear.WearActivity" />
        <activity android:name=".bookmark.BookmarkActivity"></activity>
    </application>

</manifest>